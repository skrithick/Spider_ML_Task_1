# Hello SPML seniors :)

- main.py contains the api code
- rag.py contains the rag code

- `./data/docs` contains the pdfs
- `./data/chroma_db` is the vector-db
- `./frontend/` contains html, css and javascript files